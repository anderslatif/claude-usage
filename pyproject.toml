[project]
name = "claude-usage-mac-menu"
version = "0.1.0"
description = "macOS menu bar app for Anthropic API usage and cost tracking"
requires-python = ">=3.11"
dependencies = [
    "rumps>=0.4.0",
    "requests>=2.32.3",
    "keyring>=25.6.0",
    "python-dotenv>=1.0.1",
]
